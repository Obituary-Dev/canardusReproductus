<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Canardusreproductus" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:spinnerItemStyle">@style/defaultTextStyle</item>
        <item name="android:spinnerDropDownItemStyle">@style/defaultTextStyle</item>
        <item name="buttonStyle">@color/dark_grey</item>

    </style>

    <!-- My Resources -->

    <!-- Create default title component -->
    <style name="defaultTextStyle">
        <item name="android:textColor">@color/light_grey</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/kanit</item>
    </style>

    <!-- Create default title component -->
    <style name="SubtitleTextStyle" parent="defaultTextStyle">
        <item name="android:textColor">@color/dark_grey</item>
    </style>

    <!-- Create default title component -->
    <style name="TitleTextStyle" parent="SubtitleTextStyle">
        <item name="android:textSize">24sp</item>
    </style>

    <!-- Create style for the nav view -->
    <style name="bottomNavStyle" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/kanit</item>
    </style>






</resources>